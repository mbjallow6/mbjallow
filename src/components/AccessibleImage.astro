---
// file: src/components/AccessibleImage.astro
import { Image } from 'astro:assets'

interface Props {
  src: string | ImageMetadata | { src: any; alt?: string }
  alt?: string
  title?: string
  class?: string
  width?: number
  height?: number
  loading?: 'lazy' | 'eager'
  decoding?: 'async' | 'sync' | 'auto'
  fallbackAlt?: string
  quality?: number
  format?: 'avif' | 'webp' | 'jpeg' | 'png'
  priority?: boolean
  fetchpriority?: 'high' | 'low' | 'auto'
}

const { 
  src, 
  alt, 
  title,
  class: className, 
  width = 800, 
  height = 400,
  loading = 'lazy',
  decoding = 'async',
  fallbackAlt,
  quality = 80,
  format = 'webp',
  priority = false,
  fetchpriority = 'auto',
  ...rest 
} = Astro.props

/**
 * PRODUCTION-READY: Alt text generation that NEVER returns empty string
 */
const generateIntelligentAltText = (): string => {
  if (alt && alt.trim().length > 0) {
    return alt.trim()
  }
  
  if (typeof src === 'object' && 'alt' in src && src.alt && src.alt.trim().length > 0) {
    return src.alt.trim()
  }
  
  if (fallbackAlt && fallbackAlt.trim().length > 0) {
    return fallbackAlt.trim()
  }
  
  try {
    let imageSrc: string = ''
    
    if (typeof src === 'string') {
      imageSrc = src
    } else if (typeof src === 'object' && 'src' in src) {
      imageSrc = typeof src.src === 'string' ? src.src : ''
    }
    
    if (imageSrc && imageSrc.trim().length > 0) {
      const filename = imageSrc.split('/').pop()?.split('?')[0]?.split('.')[0]
      if (filename && filename.trim().length > 0) {
        const readableText = filename
          .replace(/[-_]/g, ' ')
          .replace(/([A-Z])/g, ' $1')
          .replace(/\s+/g, ' ')
          .trim()
          .toLowerCase()
          .replace(/^\w/, c => c.toUpperCase())
        
        if (readableText && readableText.length > 0) {
          return `Image: ${readableText}`
        }
      }
    }
  } catch (error) {
    console.warn('AccessibleImage: Error extracting filename for alt text:', error)
  }
  
  return 'Descriptive image'
}

/**
 * BULLETPROOF: Image analysis that NEVER lets invalid images reach Astro Image component
 */
const analyzeImageSource = () => {
  if (!src) {
    return { type: 'invalid', isValid: false, imageSrc: null, useAstroImage: false }
  }

  // Case 1: Astro imported image (ImageMetadata) - SAFE for Astro Image
  if (typeof src === 'object' && 'src' in src && 'width' in src && 'height' in src) {
    return {
      type: 'astro-imported',
      isValid: true,
      imageSrc: src,
      useAstroImage: true
    }
  }

  // Case 2: Image object with ImageMetadata src - SAFE for Astro Image
  if (typeof src === 'object' && 'src' in src && src.src) {
    // Only use Astro Image if src is ImageMetadata (has width/height properties)
    const isImageMetadata = typeof src.src === 'object' && 'width' in src.src && 'height' in src.src
    
    return {
      type: 'object-with-src',
      isValid: true,
      imageSrc: src.src,
      useAstroImage: isImageMetadata
    }
  }

  // Case 3: String URL/path - NEVER use Astro Image for strings
  if (typeof src === 'string') {
    const trimmedSrc = src.trim()
    
    try {
      // External HTTPS URLs - Use regular img tag
      if (trimmedSrc.startsWith('https://')) {
        return {
          type: 'external-https',
          isValid: true,
          imageSrc: trimmedSrc,
          useAstroImage: false // NEVER use Astro Image for URL strings
        }
      }
      
      // External HTTP URLs - Use regular img tag
      if (trimmedSrc.startsWith('http://')) {
        console.warn(`AccessibleImage: Using insecure HTTP image: ${trimmedSrc}`)
        return {
          type: 'external-http',
          isValid: true,
          imageSrc: trimmedSrc,
          useAstroImage: false // NEVER use Astro Image for URL strings
        }
      }
      
      // Absolute paths (public folder) - Use regular img tag
      if (trimmedSrc.startsWith('/')) {
        return {
          type: 'absolute-path',
          isValid: true,
          imageSrc: trimmedSrc,
          useAstroImage: false // NEVER use Astro Image for path strings
        }
      }
      
      // Data URLs - Use regular img tag
      if (trimmedSrc.startsWith('data:image/')) {
        return {
          type: 'data-url',
          isValid: true,
          imageSrc: trimmedSrc,
          useAstroImage: false // NEVER use Astro Image for data URLs
        }
      }
      
      // CRITICAL: Handle relative paths - Convert them but DON'T use Astro Image
      if (trimmedSrc.includes('.') && /\.(jpg|jpeg|png|webp|gif|svg)$/i.test(trimmedSrc)) {
        const convertedPath = `/images/posts/${trimmedSrc.replace(/^\.\//, '')}`
        console.warn(`AccessibleImage: Converting relative path "${trimmedSrc}" to "${convertedPath}"`)
        return {
          type: 'relative-converted',
          isValid: true,
          imageSrc: convertedPath,
          useAstroImage: false // CRITICAL: NEVER use Astro Image for converted paths
        }
      }
      
    } catch (error) {
      console.error('AccessibleImage: Error validating image source:', error)
    }
  }

  return { type: 'invalid', isValid: false, imageSrc: null, useAstroImage: false }
}

const imageAnalysis = analyzeImageSource()
const altText = generateIntelligentAltText()

// Triple-check alt text is never empty
const guaranteedAltText = (altText && altText.trim().length > 0) ? altText.trim() : 'Descriptive image'
const optimizedAlt = guaranteedAltText.length > 125 ? guaranteedAltText.substring(0, 122) + '...' : guaranteedAltText
const finalAltText = (optimizedAlt && optimizedAlt.trim().length > 0) ? optimizedAlt : 'Image content'

// Loading strategy
const effectiveLoading = priority ? 'eager' : loading
const effectiveFetchPriority = priority ? 'high' : fetchpriority

// Sanitize CSS
const sanitizedClassName = className?.replace(/[<>'"]/g, '') || ''

// Development logging
if (import.meta.env.DEV && imageAnalysis.isValid) {
  console.log(`🖼️  AccessibleImage Analysis:`)
  console.log(`   Type: ${imageAnalysis.type}`)
  console.log(`   Use Astro Image: ${imageAnalysis.useAstroImage}`)
  console.log(`   Final Alt text: "${finalAltText}" (${finalAltText.length} chars)`)
  console.log(`   Loading: ${effectiveLoading} | Priority: ${priority}`)
  
  if (!alt && !fallbackAlt) {
    console.warn(`⚠️  No explicit alt text provided. Generated: "${finalAltText}"`)
  }
}
---

<!-- BULLETPROOF rendering: Never pass strings to Astro Image -->
{imageAnalysis.isValid ? (
  <figure class="accessible-image-container" role="img">
    {imageAnalysis.useAstroImage ? (
      <!-- ONLY for proper ImageMetadata objects -->
      <Image 
        src={imageAnalysis.imageSrc}
        alt={finalAltText}
        title={title}
        class={sanitizedClassName}
        width={width}
        height={height}
        loading={effectiveLoading}
        decoding={decoding}
        quality={quality}
        format={format}
        fetchpriority={effectiveFetchPriority}
        {...rest}
      />
    ) : (
      <!-- For ALL strings, URLs, and converted paths -->
      <img 
        src={imageAnalysis.imageSrc}
        alt={finalAltText}
        title={title}
        class={sanitizedClassName}
        width={width}
        height={height}
        loading={effectiveLoading}
        decoding={decoding}
        fetchpriority={effectiveFetchPriority}
        onerror="this.style.display='none'; this.nextElementSibling?.classList.remove('hidden')"
        {...rest}
      />
    )}
    
    <!-- Graceful fallback -->
    <div class="hidden image-fallback" aria-label="Image not available">
      <div class="flex items-center justify-center w-full h-32 bg-gray-100 dark:bg-gray-800 rounded border-2 border-dashed border-gray-300 dark:border-gray-600">
        <div class="text-center text-gray-500 dark:text-gray-400">
          <svg class="w-8 h-8 mx-auto mb-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 16l4.586-4.586a2 2 0 012.828 0L16 16m-2-2l1.586-1.586a2 2 0 012.828 0L20 14m-6-6h.01M6 20h12a2 2 0 002-2V6a2 2 0 00-2-2H6a2 2 0 00-2 2v12a2 2 0 002 2z"></path>
          </svg>
          <p class="text-sm">Image not available</p>
          <p class="text-xs opacity-75">{finalAltText}</p>
        </div>
      </div>
    </div>
  </figure>
) : (
  <div class="sr-only" role="img" aria-label="Invalid image source">
    Image source not provided or invalid
  </div>
)}

<style>
  .accessible-image-container {
    position: relative;
    margin: 0;
  }
  
  .image-fallback {
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    z-index: 1;
  }
  
  .sr-only {
    position: absolute;
    width: 1px;
    height: 1px;
    padding: 0;
    margin: -1px;
    overflow: hidden;
    clip: rect(0, 0, 0, 0);
    white-space: nowrap;
    border: 0;
  }
  
  .accessible-image-container img:focus {
    outline: 2px solid var(--theme-accent, #3b82f6);
    outline-offset: 2px;
  }
</style>
